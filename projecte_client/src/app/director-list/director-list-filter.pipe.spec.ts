import { DirectorListFilterPipe } from './director-list-filter.pipe';

describe('DirectorListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectorListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
